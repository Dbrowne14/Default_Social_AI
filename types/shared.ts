// shared types

export type LinkItem = {
  label: string;
  href: string;
  external?: boolean;
}

export type TitleDescription = {
  title: string;
  description: string;
};

export type TitleLine = {
  text: string;
  emphasis?: boolean;
};